{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d react-app ]; then cd react-app; fi; npm ci",
  "buildCommand": "if [ -d react-app ]; then cd react-app; fi; npm run build",
  "outputDirectory": "react-app/dist",
  "framework": null
}
